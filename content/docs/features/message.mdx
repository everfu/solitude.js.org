---
title: "Message Board"
description: "Create a message page for visitor notes and lightweight interactions."
---
## Purpose

Create a message page for visitor notes and lightweight interactions.

## Create the page

Create a page under `source/message/index.md` and set a clear title.

```md
---
title: Message Board
date: 2026-01-01
type: message
---
```

## Create the data file

Put repeatable content in `source/_data/message.yml`.

```yaml
items:
  - name: Example item
    description: Replace this with your own content.
    link: https://example.com
```

## Theme configuration

Enable the page module in the theme configuration when the feature requires a switch.

```yaml
pages:
  message:
    enable: true
    data: message.yml
```

## File structure

<Files>
  <File name="Blog Directory" />
  <File name="source" />
  <File name="_data" />
  <File name="message.yml" />
  <File name="message" />
  <File name="index.md" />
</Files>

## Field reference

- `name`: display name.
- `description`: short explanatory text.
- `link`: optional external or internal URL.
- `cover`: optional image path when the page layout supports covers.
- `tags`: optional labels used for grouping or filtering.

<Callout type="success">
Start with one or two items, confirm the page renders, then expand the data file.
</Callout>
